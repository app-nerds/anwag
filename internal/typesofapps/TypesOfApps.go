package typesofapps

const (
	EmptyApp string = "Empty application"
	ApiApp   string = "API application (REST + GraphQL)"
	JSApp    string = "JavaScript web application (nerdwebjs + REST + GraphQL)"
	CronApp  string = "CRON application"
)
