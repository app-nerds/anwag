<template>
	<header>
		<b-navbar toggleable="lg" type="dark" variant="dark" fixed="top">
			<b-navbar-brand href="/">{{.Title}}</b-navbar-brand>
			<b-navbar-toggle target="nav-collapse"></b-navbar-toggle>

			<b-collapse id="nav-collapse" is-nav>
				<b-navbar-nav>
					<li class="nav-item" :class="{ active: isActive('home') }">
						<router-link class="nav-link" :to="{ name: 'home' }"
							>Home
							<span v-if="isActive('home')" class="sr-only"
								>(current)</span
							></router-link
						>
					</li>
					<li class="nav-item" :class="{ active: isActive('pagetwo') }">
						<router-link class="nav-link" :to="{ name: 'pagetwo' }"
							>Page Two
							<span v-if="isActive('pagetwo')" class="sr-only"
								>(current)</span
							></router-link
						>
					</li>
				</b-navbar-nav>

				<b-navbar-nav class="ml-auto">
					<b-nav-text right
						><span>Version: {{ `{{ version }}` }}</span>
					</b-nav-text>
				</b-navbar-nav>
			</b-collapse>
		</b-navbar>
	</header>
</template>

<style>
.version {
	color: white;
}
</style>

<script>
/*
 * Copyright © {{.Year}}. {{.CompanyName}} All Rights Reserved
 */

export default {
	data() {
		return {
			version: "",
		};
	},

	async created() {
		this.version = await this.versionService.getVersion();
	},

	methods: {
		isActive(routeName) {
			return this.$route.name === routeName;
		},
	},
};
</script>
